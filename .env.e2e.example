# Optional: use a deployed site instead of the local static server.
CHEF_E2E_BASE_URL=https://chef-jarvis.pages.dev

# Optional authenticated read-only and route-mocked named-recipe account.
# The authenticated suite is explicitly skipped when either value is absent.
# Use a dedicated account with completed onboarding; never use a personal or
# production administrator login.
CHEF_E2E_EMAIL=chef-jarvis-e2e@example.com
CHEF_E2E_PASSWORD=replace-with-a-test-only-password
